/**
 * Analyzer Service
 * Convenience re-export of scanService for backward compatibility
 * and for components that reference the "analyzer" feature name.
 */
export { scanService as analyzerService } from './scan.service';
export type { } from '@/types/common';